# Eonic Website

Marketing landing page for the Eonic Baseline health protocol, built with Next.js 14.

## Getting started

Install dependencies and run the dev server:

```bash
npm install
npm run dev
```

The site is powered by the `app` directory and includes placeholder legal pages at `/terms` and `/privacy`.
