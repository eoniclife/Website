export async function sendReminderMessage(number: string, message: string) {
  console.log("[V1 STUB] Would send WhatsApp to:", number, "message:", message);
  return { success: true, stub: true };
}
